server:

  port: 8081



# JDBC

spring:

  datasource:

    driver-class-name: com.mysql.cj.jdbc.Driver

    url: jdbc:mysql://localhost:3306/restful

    username: jaewon

    password: Zmfhdnwm0=

  # JPA

  jpa:

    database-platform: org.hibernate.dialect.MySQL8Dialect

    properties:

      hibernate:

        format_sql: true

    hibernate:

      ddl-auto: create # DTO(Object)---ORM---TABLE(Entity, R)

    show-sql: true